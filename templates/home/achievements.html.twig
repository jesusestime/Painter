<section class="about section bg-light">
	<div class="container">
		<h2>Last Achievements</h2>
        <p>Far far away, behind the word mountains, far from the countries Vokalia and Consonantia, there live the
        blind texts. Separated they live in Bookmarksgrove right at the coast of the Semantics
        </p>
		<hr class="mt-5">
    </div>
<section>
{% for p in paints %}
	<section class="about pricing-table section-card bg-gray">
		<div class="container pricing-item p-0 ">
			<div class="row  card-body">
				<div class="col-lg-6">
					<div class="about-img">
						<img class="img-fluid" src="{{ p.file }}">
					</div>
				</div>
				<div class="col-lg-6 mt-lg-0 ">
					<div class="pl-0 pl-lg-4 animated fadeInUp">
						
						<h2>{{ p.name }}</h2>
						<small class=""><i class="ion-calendar"></i>  Posted {{ p.createdat |ago}}</small>
						<p>{{ p.description|u.truncate(200,'...',false) }}</p>
						<a href="{{ path('app_achievement_detail',{'slug': p.slug}) }}" class="btn btn-small mt-0">View paint</a>
					</div>
				</div>
			</div>
		</div>
	<section>
{% endfor %}
